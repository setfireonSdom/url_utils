from setuptools import setup, find_packages

setup(
    name='url_utils',
    version='0.1.0',
    description='A simple library for working with URLs',
    author='Your Name',
    author_email='your.email@example.com',
    packages=find_packages(),
)
